// 4. Store the number values 50, 100, and 500 each in a separate variable. Then add the 3 variables using + and
//  assign that sum to a fourth variable. Finally print the sum using console.log.

// here's the first variable assignment you'll need
// you'll need to to assign the other 2 numbers yourself
var firstNumber = 50;
var secondNumber = 100;
var thirdNumber = 500;
// add them together using + and store them in the sum variable
var sum = firstNumber + secondNumber + thirdNumber;

// finally, print the sum
console.log(sum);
